import 'package:flutter/material.dart';

class TharwatSamyCourse2 extends StatelessWidget {
  const TharwatSamyCourse2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      darkTheme: ThemeData(),
      themeMode: ThemeMode.dark,
      home: const Center(
        child: Text("TharwatSamyCourse2"),
      ),
    );
  }
}
