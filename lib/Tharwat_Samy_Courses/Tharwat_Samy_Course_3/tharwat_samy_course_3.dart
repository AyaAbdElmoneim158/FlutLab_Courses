import 'package:flutter/material.dart';

class TharwatSamyCourse3 extends StatelessWidget {
  const TharwatSamyCourse3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      darkTheme: ThemeData(),
      themeMode: ThemeMode.dark,
      home: const Center(
        child: Text("TharwatSamyCourse3"),
      ),
    );
  }
}
