import 'package:flutter/material.dart';

class TharwatSamyCourse1 extends StatelessWidget {
  const TharwatSamyCourse1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      darkTheme: ThemeData(),
      themeMode: ThemeMode.dark,
      home: const Center(
        child: Text("TharwatSamyCourse1"),
      ),
    );
  }
}
